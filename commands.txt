cd week11_exam

echo "# week11_exam" >> README.md

git init

git add README.md

git commit -m "first commit"

git branch -M main

git remote add origin https://github.com/git-gad/week11_exam.git

git push -u origin main

git checkout -b dev

git clone https://github.com/yosefanaliza/streamlit_minikube_app

minikube start

kubectl config get-contexts

docker build -t streamlit-app:latest .

docker run -d -p 8501:8501 streamlit-app:latest

docker push gaddock/week11_exam:streamlit-app

docker delete image gaddock/week11_exam:streamlit-app

docker pull gaddock/week11_exam:streamlit-app

docker images

git add .

git commit -m 'add dockerfile'

git push --set-upstream origin dev

